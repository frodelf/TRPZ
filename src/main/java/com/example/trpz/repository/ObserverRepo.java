package com.example.trpz.repository;

import com.example.trpz.entity.Observer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ObserverRepo extends JpaRepository<Observer, Long> {
}
